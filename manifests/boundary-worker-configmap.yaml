apiVersion: v1
kind: ConfigMap
metadata:
  name: boundary-worker-config
  namespace: aniket-test
data:
  boundary-worker.hcl: |
    disable_mlock = true

    worker {
      initial_upstreams = ["boundary:9201"]

      tls_disable = true   # ✅ REQUIRED FOR v0.21 (THIS FIXES THE ERROR)

      auth_storage_path = "/boundary/auth"

      # ACTIVATION TOKEN (one-time use)
      controller_generated_activation_token = "neslat_2KrZsJtGUesQi9zA5njLJ1bWYWyyy49U8W1Es4LbMF8hGp1YpupNri6JdBfZvDHeeErcm2Eii2j5biR8YXaMoWjLnDgLA"
    }

    listener "tcp" {
      address = "0.0.0.0:9202"
      purpose = "proxy"
    }
